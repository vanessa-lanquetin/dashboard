{
  "name": "dashboard",
  "version": "1.0.3",
  "description": "",
  "main": "app.js",
  "scripts": {
    "serve": "cross-env HTTP_PORT=6872 stack-monitor ./stack.js",
    "build": "cd front && npm run build",
    "postinstall": "cd front && npm i && cd ../server && npm i",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@iryu54/stack-monitor": "^1.11.18",
    "cross-env": "^7.0.3",
    "dotenv": "^16.3.1"
  }
}
